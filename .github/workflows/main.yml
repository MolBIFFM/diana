on:
  push:
    branches:
    - main
jobs:
  mypy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip3 install mypy
      - run: mypy --ignore-missing-imports diana/diana.py
